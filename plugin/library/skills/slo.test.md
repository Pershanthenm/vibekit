---
skill: slo
triggers-on: ["define an error budget for the checkout API", "set up burn rate alerts for the search service", "what should the service level objective be for login", "write SLOs for the payments platform", "pick a reliability target for the notifications worker"]
must-not-trigger-on: ["the reports endpoint is slow, find the bottleneck", "reduce the noisy alerts from the disk usage monitor", "add a kill switch for the new checkout flow"]
---
