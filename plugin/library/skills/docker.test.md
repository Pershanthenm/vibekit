---
skill: docker
triggers-on: ["the dockerfile for the api builds in nine minutes, speed it up", "write a docker compose file with postgres and redis for local dev", "containerize the report worker", "turn this into a multi-stage build so the image is smaller"]
must-not-trigger-on: ["add cursor pagination to the invoices endpoint", "fix the flaky playwright spec for checkout", "make the hero image responsive"]
---
