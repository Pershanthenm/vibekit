---
skill: frontend-react
triggers-on: ["build a react component for the pricing table", "migrate the dashboard page to next.js server components", "restyle the pricing card with tailwind so the variants share one class list", "extract the polling logic into a react hook"]
must-not-trigger-on: ["add an index on orders.customer_id", "set up the github actions workflow for the api", "write a playwright test for checkout"]
---
