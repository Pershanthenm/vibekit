---
skill: threat-model
triggers-on: ["threat model the new file upload service", "run stride on the payment flow before we build it", "what is the attack surface of the admin API", "give each finding a dread score and rank them"]
must-not-trigger-on: ["poke holes in the new payment handler", "the login page is down for all users, what do we do", "write the failing test for the session timeout"]
---
