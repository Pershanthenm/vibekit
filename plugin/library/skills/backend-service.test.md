---
skill: backend-service
triggers-on: ["design the rest api for the orders service", "add a new api endpoint that returns the invoices for a customer", "add rate limiting to the login and password reset routes", "add cursor pagination to the users list"]
must-not-trigger-on: ["write the failing test for AC-2", "make the hero section responsive on mobile", "optimise the dockerfile for the worker image"]
---
