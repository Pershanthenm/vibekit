---
skill: api-design
triggers-on: ["review the api design for the new orders endpoints", "is renaming customerId in the response a breaking change", "check the openapi spec before we publish v2", "settle the endpoint naming for the invoices resource"]
must-not-trigger-on: ["write integration tests for the users endpoints", "add an index on orders.customer_id", "give this pull request an adversarial review before merge"]
---
