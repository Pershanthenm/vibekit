---
skill: database-design
triggers-on: ["propose a schema design for subscriptions and invoices", "review the database schema before the first migration", "model the data for multi-tenant projects", "add a new table for audit events"]
must-not-trigger-on: ["the orders report is a slow query, speed it up", "rename a column without downtime", "rotate the database credentials"]
---
