---
skill: ci-pipeline
triggers-on: ["set up a ci pipeline for the new python service", "add a github actions job that runs lint and tests on every pull request", "the gitlab ci config deploys before tests finish, fix the stage order", "add a manual approval to the deploy pipeline for production"]
must-not-trigger-on: ["write the failing test for the coupon rule", "reduce the size of the worker container image", "make the modal work with keyboard navigation"]
---
