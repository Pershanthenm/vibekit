---
skill: performance-profiling
triggers-on: ["the reports endpoint is a slow endpoint, find the bottleneck", "hunt the memory leak in the worker process", "generate a flamegraph for the search API under load", "p99 latency doubled after the last deploy", "run a load test against staging before the launch", "compare heap snapshot files from the leaking worker"]
must-not-trigger-on: ["define an error budget for the checkout API", "find the stale flags before the release freeze", "write tests for the cart service before the code"]
---
