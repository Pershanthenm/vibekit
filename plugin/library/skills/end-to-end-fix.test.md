---
skill: end-to-end-fix
triggers-on: ["the auth module is broken since the prisma upgrade, make it work", "the export feature is broken across three screens", "the notifications module needs to work properly before launch", "repair the whole feature, not just the failing screen"]
must-not-trigger-on: ["poke holes in the new payment handler", "rank the tech debt in the billing service", "verify apis before writing the S3 client"]
---
