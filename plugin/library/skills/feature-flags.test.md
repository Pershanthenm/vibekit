---
skill: feature-flags
triggers-on: ["ship the new pricing page behind a flag", "add a kill switch for the recommendations service", "write a rollout plan for the redesigned checkout", "find the stale flags before the release freeze", "add a feature flag for the export button"]
must-not-trigger-on: ["define an error budget for the checkout API", "audit our npm dependencies for vulnerabilities", "rotate the leaked database credentials"]
---
